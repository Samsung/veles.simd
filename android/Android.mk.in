LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)

LOCAL_MODULE := @PACKAGE_NAME@

LOCAL_SRC_FILES := 

LOCAL_CFLAGS := -std=c99 -fopenmp -Wall -Wextra -Wno-comment -Wformat-security -I@abs_top_srcdir@ -I@abs_top_srcdir@/inc -I@abs_top_builddir@

LOCAL_CPPFLAGS := -std=c++0x -fno-deduce-init-list -Winvalid-pch -Wold-style-cast -Wnon-virtual-dtor -frtti -fexceptions

LOCAL_LDLIBS := -fopenmp

include $(BUILD_SHARED_LIBRARY)
